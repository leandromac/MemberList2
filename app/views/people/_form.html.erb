<%= form_for(@person) do |f| %>
  <% if @person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@person.errors.count, "error") %> prohibited this person from being saved:</h2>

      <ul>
      <% @person.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="row">
    <div class="col-md-4"></div>
    <div class="col-md-4">
      <div class="form-group">
        <%= f.text_field :name, class: "form-control", placeholder: "Name" %>
      </div>
      <div class="form-group">
        <%= f.email_field :email, class: "form-control", placeholder: "Email" %>
      </div>
      <div class="form-group">
        <%= f.phone_field :phone, class: "form-control", placeholder: "Phone" %>
      </div>
      <div class="form-group">
        <%= f.text_field :location, class: "form-control", placeholder: "Location" %>
      </div>
      <div class="form-group">
        <%= f.url_field :site, class: "form-control", placeholder: "Site" %>
      </div>
      <div class="form-group">
        <%= f.url_field :facebook, class: "form-control", placeholder: "Facebook" %>
      </div>
      <div class="form-group">
        <%= f.url_field :linkedin, class: "form-control", placeholder: "Linkedin" %>
      </div>
      <div class="form-group">
        <%= f.url_field :github, class: "form-control", placeholder: "Github" %>
      </div>
      <div class="form-group">
        <%= f.text_field :skill, class: "form-control", placeholder: "Skill" %>
      </div>
      <div class="form-group">
        <%= f.text_area :about, class: "form-control", placeholder: "About me" %>
      </div>
      <div class="form-group">
        <%= f.text_field :category_id, class: "form-control", placeholder: "Category ID" %>
      </div>
        <div class="actions">
      <%= f.submit "Save", class: 'btn btn-primary' %>
    </div>
  <% end %>
    </div>
    <div class="col-md-4"></div>
  </div>

